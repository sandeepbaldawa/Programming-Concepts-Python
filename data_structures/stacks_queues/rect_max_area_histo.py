"""
1. Add to stack if current value is equal or bigger than top of the stack.
2. Else keep removing from the stack till a number smaller than or equal to current value is found
3. Everytime you remove calculate the area
if stack is empty:
    area = input[top] * i
else:
    area = input[top] * (i-stacktop-1)
    
    
i => Just the index of the bar item in histo 

"""
