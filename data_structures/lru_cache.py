# Implement LRU Cache 
# Idea is we should optimize both on reads and writes
# For Reads O(1) as always Hash is the best
# For optimized on writes to evict the LRU double linked list can be used
# This gives O(1) for both operations
